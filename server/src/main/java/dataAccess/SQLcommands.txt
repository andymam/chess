CREATE TABLE IF NOT EXISTS user (username VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, PRIMARY KEY (username));


schemas, right click on my table, select rows - can mess with commands

run \sql in terminal to get into sql mode from JS